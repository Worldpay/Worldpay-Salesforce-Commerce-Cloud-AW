<isdecorate template="common/worldpayPages">
    <isscript>
        var assets = require('*/cartridge/scripts/assets');
        assets.addJs('/js/redirect.js');
    </isscript>
    <form id="challengeForm" name="challengeForm" method="POST" action="${pdict.URL}">
        <input type="hidden" name="JWT" value="${pdict.JWT}" />
        <input type="hidden" name="MD" value="orderNo=${pdict.orderNo}"/>
    </form>
</isdecorate>
